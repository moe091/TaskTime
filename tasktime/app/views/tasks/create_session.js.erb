reset_timer();
console.log("CREATE SESSION JS")
$('#sessions-partial').load('/tasks/show_sessions/<%= @task.id %>');
            console.log("redraw");

$('.task-completed').html("<%= format_minutes(@task.completed) %>" + " Hours");
