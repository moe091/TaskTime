<% session = Session.new %>
<% session.task = @task %>
<% session.duration = 0 %>
<% session.start = 0 %>
<% session.end = 0 %>

<%= @duration %>
<!-- Move the above code to controller when New Session button is set up -->
<div class="new-session session-col">
			<div class="session-title new-title">
						<%= session.task.name %> <br>
					</div>

					<div class="new-content session-content"> <!-- SESSION CONTENT -->

					
						<div class="new-circle circle">
							<div class="circle-cover">
							<div class="circle2">
								
							</div>
							</div>
							<span class="session-duration session-duration-new"> 00:00:00</span>
						</div>

						<div class="new-buttons">
							<span class="but-wrap">
								<%= button_to "Start", {:action => "start_timer", :controller => "tasks", :start => session.start}, :method => :post, :remote => true, class: "btn btn-success btn-start session-btn btn-lg" %>

								<button type="button" class="btn btn-danger btn-stop session-btn btn-lg" onclick="stopTimer(<%= @task.id %>)">Stop</button>
							</span>

						</div>


						<div class="new-text new-note session-text session-note"> <%= session.note %> </div>


						<span class="new-links">

							<%= link_to "Delete", {:action => "start_timer", :controller => "tasks"}, :method => :post, :remote => true, class: "new-delete" %> - 
							<a id="save_new" href="#">Save</a>
							<!--
							 <= link_to "Save", 
								{:action => "start_timer", :controller => "tasks", :task_id => @task.id,
								:start => session.start,
								:duration => session.duration,
								:end => session.end},
								:method => :post,
								:remote => true, 
								class: "new-save" >
							-->

						</span>

						<!--
						<button type="button" class="btn btn-success btn-start session-btn btn-lg" onclick="startTimer()">Start</button>

						<= link_to "Save", 
								{:action => "start_timer", :controller => "tasks", :task_id => @task.id,
								:start => session.start,
								:duration => session.duration,
								:end => session.end},
								:method => :post,
								:remote => true, 
								class: "new-save" >


							click start
								if start time isn't set -> set start time
								start timer counting

							click save
								message (stop timer before saving)

							click stop
								stop timer
								set / update end time
								set / update duration

							click save
								set note
								send params to controller
								create new session and save it, add to task
								update page - ajax


						-->


					</div> 		<!-- SESSION CONTENT -->

		</div>